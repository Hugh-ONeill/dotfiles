#################
### AUTOSTART ###
#################

exec-once = ~/.config/hypr/scripts/xdg-desktop-portal-reset.sh # reset XDPH for screenshare
exec-once = uwsm-app start dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP # for XDPH
exec-once = dbus-update-activation-environment --systemd --all # for XDPH
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP # for XDPH
exec-once = systemctl --user start hyprpolkitagent # authentication dialogue for GUI apps
exec-once = dunst # notification daemon
exec-once = waybar
#exec-once = blueman-applet # systray app for BT
#exec-once = nm-applet --indicator # systray app for Network/Wifi
#exec-once = waypaper --restore
exec-once = hyprpaper
exec-once = clipse -listen
#exec-once = wl-paste --type text --watch cliphist store # clipboard store text data
#exec-once = wl-paste --type image --watch cliphist store # clipboard store image data
exec-once = hypridle
#exec-once = hyprpm reload -nn
exec-once = hyprctl setcursor 24
