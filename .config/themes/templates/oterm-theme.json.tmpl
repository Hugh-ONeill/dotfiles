{
  "name": "themes-system",
  "primary": "${ACCENT}",
  "secondary": "${ACCENT_SECONDARY}",
  "accent": "${ACCENT_BRIGHT}",
  "foreground": "${TEXT}",
  "background": "${BASE}",
  "surface": "${SURFACE0}",
  "panel": "${MANTLE}",
  "success": "${SEM_OK}",
  "warning": "${SEM_WARN}",
  "error": "${SEM_ERR}",
  "dark": true
}
