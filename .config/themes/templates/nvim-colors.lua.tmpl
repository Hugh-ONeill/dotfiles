-- Auto-generated nvim color palette for catppuccin
-- Source: theme system (~/.config/themes/)
-- Do not edit -- regenerate with: theme generate <name>

return {
  separator_style = "${STYLE_WAYBAR}",

  color_overrides = {
    all = {
      -- structural
      crust    = "${CRUST}",
      mantle   = "${MANTLE}",
      base     = "${BASE}",
      surface0 = "${SURFACE0}",
      surface1 = "${SURFACE1}",
      surface2 = "${SURFACE2}",
      overlay0 = "${OVERLAY0}",
      overlay1 = "${OVERLAY1}",
      overlay2 = "${OVERLAY2}",
      subtext0 = "${SUBTEXT0}",
      subtext1 = "${SUBTEXT1}",
      text     = "${TEXT}",

      -- syntax / accent
      red       = "${ANSI_RED}",
      maroon    = "${ANSI_RED_BRIGHT}",
      green     = "${ANSI_GREEN}",
      yellow    = "${ANSI_YELLOW}",
      peach     = "${ANSI_YELLOW_BRIGHT}",
      blue      = "${ANSI_BLUE}",
      sapphire  = "${ANSI_BLUE_BRIGHT}",
      teal      = "${ANSI_CYAN}",
      sky       = "${ANSI_CYAN_BRIGHT}",
      pink      = "${ANSI_MAGENTA}",
      flamingo  = "${ANSI_MAGENTA_BRIGHT}",
      mauve     = "${ACCENT}",
      lavender  = "${ACCENT_SECONDARY}",
      rosewater = "${ACCENT_TERTIARY}",
    },
  },

  highlight_overrides = {
    all = function(C)
      return {
        -- diagnostics (use semantic colors, may differ from ansi in some themes)
        DiagnosticError = { fg = "${SEM_ERR}" },
        DiagnosticWarn  = { fg = "${SEM_WARN}" },
        DiagnosticInfo  = { fg = "${SEM_INFO}" },
        DiagnosticHint  = { fg = "${SEM_INFO}" },
        DiagnosticOk    = { fg = "${SEM_OK}" },

        DiagnosticUnderlineError = { sp = "${SEM_ERR}", style = { "undercurl" } },
        DiagnosticUnderlineWarn  = { sp = "${SEM_WARN}", style = { "undercurl" } },
        DiagnosticUnderlineInfo  = { sp = "${SEM_INFO}", style = { "undercurl" } },
        DiagnosticUnderlineHint  = { sp = "${SEM_INFO}", style = { "undercurl" } },
        DiagnosticUnderlineOk    = { sp = "${SEM_OK}", style = { "undercurl" } },

        -- dashboard gradient
        DashGrad0  = { fg = "${GRADIENT_0}" },
        DashGrad1  = { fg = "${GRADIENT_1}" },
        DashGrad2  = { fg = "${GRADIENT_2}" },
        DashGrad3  = { fg = "${GRADIENT_3}" },
        DashGrad4  = { fg = "${GRADIENT_4}" },
        DashGrad5  = { fg = "${GRADIENT_5}" },
        DashGrad6  = { fg = "${GRADIENT_6}" },
        DashGrad7  = { fg = "${GRADIENT_7}" },
        DashGrad8  = { fg = "${GRADIENT_8}" },
        DashGrad9  = { fg = "${GRADIENT_9}" },

        -- indent guide gradient (reuses gradient, subtle on thin chars)
        IndentGrad0  = { fg = "${GRADIENT_0}" },
        IndentGrad1  = { fg = "${GRADIENT_1}" },
        IndentGrad2  = { fg = "${GRADIENT_2}" },
        IndentGrad3  = { fg = "${GRADIENT_3}" },
        IndentGrad4  = { fg = "${GRADIENT_4}" },
        IndentGrad5  = { fg = "${GRADIENT_5}" },
        IndentGrad6  = { fg = "${GRADIENT_6}" },
        IndentGrad7  = { fg = "${GRADIENT_7}" },
        IndentGrad8  = { fg = "${GRADIENT_8}" },
        IndentGrad9  = { fg = "${GRADIENT_9}" },
        IndentScope  = { fg = "${ACCENT}" },
      }
    end,
  },
}
